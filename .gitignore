# opencode/ — the cloned repo (never committed)
opencode/

# dist/ — build output (never committed)
dist/

# .env — user's local environment (secrets/config)
.env

# .tasks/ - tasks files for working on this repo
.tasks/

# Editor temps
*.swp
*.swo
*~

# macOS junk
.DS_Store
